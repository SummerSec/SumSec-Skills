# STEP {n}: {Step 名称}

**⚠️ 路径一致性要求**：本 STEP 中的所有 `{progress_file_path}` 占位符必须替换为 STEP 1 生成的实际 Progress 文件绝对路径。

## 1. 校验规则

**前置校验**：
- ✅ 使用 Read 工具读取进度文件末尾，验证 STEP {n-1} 完成标记存在
- ✅ {其他前置条件}

**后置校验**：
- ✅ {后置校验条件 1}
- ✅ {后置校验条件 2}

## 2. 核心执行流程

### 步骤 1: {子步骤名称}

**目标**：{一句话目标}

**执行命令**：
```bash
{具体可执行的命令}
```

**记录**：使用 Edit 工具在进度文件末尾追加命令和执行结果。

### 步骤 2: {子步骤名称}

**目标**：{一句话目标}

**执行命令**：
```
{具体可执行的命令或 Read 操作}
```

<!-- 根据需要添加更多子步骤 -->

## 3. 完成标准与验收

**STEP {n} 完成标准**：
1. ✅ {完成标准 1}
2. ✅ {完成标准 2}

**失败处理**：
如果 {失败条件}，需要：
1. 在 Progress 文件中详细记录已尝试的操作
2. 分析可能的原因和下一步建议
3. {是否需要标记为人工介入}

---

## 4. 记录格式（追加到进度文件）

```
**{分析名称}**: {分析结论}
**关键发现**: {发现内容}
**代码证据**: `[{File}:{Line}]` {证据描述}
```