# 建议工具（环境配置参考）

以下为原先写在 `SKILL.md` YAML 中的工具提示，供 Claude Code、Cursor 等客户端配置时参考；**是否开放以当前环境策略为准**。

- Read  
- Write  
- Edit  
- Grep  
- Glob  
- AskUserQuestion  
- exec（需要在本机执行 `scripts/` 下 Python 时使用）
